server:
  port: 8082

spring:
  application:
    name: QUESTION-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/quiz
    username: mahesh
    password: mahesh

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update