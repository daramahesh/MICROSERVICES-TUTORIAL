server:
  port: 1435

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/daramahesh/microservices-config
          clone-on-start: true